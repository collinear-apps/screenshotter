node_modules/
# Anchored to the repo root so these match ONLY the build/capture artifacts —
# NOT source dirs like src/output/ (an unanchored `output/` wrongly ignored it).
/dist/
/output/
*-screenshots.zip
*.log
.DS_Store
/.auth/
